1.联合登陆工作的难点就是搭建一个简单易维护的架构。每个界面会调一个服务端接口（接口相同，但是参数不同），而且很多界面会调用上一个界面的接口，而且比如重发验证码。在这里我将对服务器的访问设计成一个一个的session，所有的session共享一个sessioncontext，
  所有联合登陆用到的数据都存在context中，而且各个session形成一个list，可以方便的回溯上一个session。由于访问的是同一个接口，那么返回值的处理逻辑都是相同的，因此把对返回值的处理提升到各个congtroller的公共基类中。